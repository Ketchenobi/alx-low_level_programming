#include <stdio.h>
/**
 * main. A program that prints an output with a puts function
 * return: (0) (Success)
 */
int main(void)
{
	/* using the puts function*/
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
