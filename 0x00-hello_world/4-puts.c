#include <stdio.h>
/**
 * main -main entry point
 * A program that prints an output with a puts function
 * The main function of the program.
 *
 * @return 0 on successful execution
 * return: (0) (Success)
 *  @return 0 on success
 */
int main(void)
{
/* Write a C program that prints using the puts function*/
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
